# Build-time configuration for Vite (loaded at dev-server start and during builds)
# Vite injects VITE_* values into import.meta.env. Changing OS env after the dev
# server starts will NOT update a running bundle.

# Feature flags
VITE_USE_MOCKS=true

# URLs (frontend-visible; no secrets)
VITE_NOTAM_URL=https://example.com/notams
VITE_DRONE_URL=https://example.com/drones
VITE_SENSORS_URL=https://example.com/sensors
VITE_MAP_WMTS_URL=https://example.com/wmts
VITE_ADSB_MODE=live
VITE_ADSB_BASE_URL=https://api.airplanes.live/v2
VITE_ADSB_CENTER_LAT=58.5953
VITE_ADSB_CENTER_LON=25.0136
VITE_ADSB_RADIUS_NM=250

# Polling intervals (ms)
VITE_POLL_INTERVAL_MS=5000
VITE_POLL_NOTAM_MS=60000
VITE_POLL_ADSB_MS=10000
VITE_POLL_DRONES_MS=1000
VITE_POLL_SENSORS_MS=1000
