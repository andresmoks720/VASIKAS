# Mock API

Simple Express server to mock backend responses during development.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run development server:
   ```bash
   npm run dev
   ```

The server listens on port 8787 (default) or `MOCK_API_PORT`.
Health check: http://localhost:8787/health
