# VASIKAS
eks see esimene...

## Mock API
This project includes an optional mock API server for development.

- **Mock API Port**: 8787 (default). Override with `MOCK_API_PORT`.
- **Run Mock API only**: `npm run dev:mock-api`
- **Run App + Mock API**: `npm run dev:all`